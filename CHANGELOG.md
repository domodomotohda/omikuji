# どもどもおみくじ（Vercel版）変更履歴

## プロジェクト識別情報

| 項目 | 値 |
|---|---|
| リポジトリ | `domodomotohda/omikuji`（GitHub） |
| 公開先 | Vercel（`main` ブランチを本番として自動デプロイ） |
| ファイル構成 | `index.html` 1枚のみ。ビルド手順なし、npm パッケージなし |
| 外部依存 | Tailwind CSS（Play CDN）／ canvas-confetti（jsDelivr）／ Google Fonts（Yusei Magic）／ GA4（`G-NPSH4SKZ5E`） |
| サーバー | **なし。** 完全な静的ページ |
| 保存先 | ブラウザの `localStorage`（端末の中だけ。サーバーには何も送らない） |

**GAS版の `56_おみくじ` とは別物。** あちらはスプレッドシートに履歴を書き込む研修教材で、
こちらは Vercel に置いた静的HTML1枚。コードは共有していない。

<!-- ここから下に、新しいものが上になるように追記 -->

## 2026-08-31 — 履歴のバックアップ（ファイルへの書き出し・復元）を追加

**作業環境**: Claude Code on the web（クラウドセッション）。ブランチ `claude/expand-claude-code-usage-p1v3kl`。

**目的**

「履歴データをPCにバックアップできないか」という依頼への対応。

**前提の確認（ここが今回の要点）**

履歴は **Vercelのサーバーには一切保存されていない**。引いた人それぞれのブラウザの
`localStorage`、つまり **その人の端末の中だけ** にある。したがって、

- **サーバー側から履歴を吸い上げる方法は存在しない**（元データが無い）
- バックアップできるのは **自分の端末に入っている自分の履歴だけ**
- 端末を替える・ブラウザの閲覧データを消す・機種変更する、のいずれでも履歴は消える

そこで「利用者が自分で、自分の履歴をファイルに書き出して保存する」形にした。

### 変更点

変更したのは **`index.html` のみ**。

**① 履歴モーダルのボタンを2段構成に変更**

| 段 | ボタン | 動作 |
|---|---|---|
| 1段目 | `💾 バックアップを保存` | 履歴を **JSON** ファイルとして保存。**復元に使えるのはこれだけ** |
| 1段目 | `📊 Excel用に書き出す` | 履歴を **CSV** で保存。閲覧・集計用（復元には使えない） |
| 1段目 | `📂 バックアップから復元` | 保存したJSONを読み込み、いまの履歴に**足す** |
| 2段目 | `履歴を消す` / `閉じる` | 従来どおり |

ファイル名は `omikuji-history-<YYYY-MM-DD>.json` / `.csv`。
履歴0件のときは、書き出し2つを `disabled` にして半透明にする（復元は0件でも押せる）。

**② `downloadFile()` を追加**

`Blob` と `URL.createObjectURL()` でファイルを作り、`<a download>` を一時的に生成して
クリックさせる。使い終わったURLは `URL.revokeObjectURL()` で解放する。

**③ CSVの作り方**

- 値は必ず二重引用符で囲み、値の中の二重引用符は2個に増やす。
  **名前にカンマや二重引用符が入っても列がずれない**（後述のとおり検証済み）
- 先頭に **BOM（`﻿`）を付ける**。これが無いとExcelが日本語を文字化けさせる
- 改行は `\r\n`

**④ 復元の安全策**

| 状況 | 挙動 |
|---|---|
| JSONとして壊れている | `ファイルを読めませんでした` と表示。**既存の履歴は変えない** |
| おみくじ以外のJSON | `おみくじのバックアップではありません`。**既存の履歴は変えない** |
| 同じファイルを2回読む | 日時 `at` が一致する行を除くので**二重に増えない**（`追加0件` と表示） |
| 形式の合わない行が混在 | その行だけ捨てて、通る行を取り込む |

取り込み後は日時の新しい順に並べ替え、上限200件で切る（`historyStore.replaceAll()`）。

**⑤ `historyStore.replaceAll()` を追加**

保存まわりは引き続き `historyStore` の中だけに閉じている。サーバーDBへ移す際に
触る場所が増えていない。

### 検証

Chromium（Playwright、390×844）で確認した。

| 確認したこと | 結果 |
|---|---|
| 12件をJSONに書き出す | `omikuji-history-2026-08-31.json` / `app=domodomo-omikuji` `count=12` |
| CSVに書き出す | BOMあり。1行目は 日時／名前／モード／運勢 の4列 |
| 名前にカンマと二重引用符を含めてCSV出力 | 正しく囲まれ、列がずれないことを確認 |
| 履歴を全消去 → JSONから復元 | 0件 → 12件（`12件を取り込みました`） |
| 同じJSONをもう一度読む | 12件のまま（`すべて取り込み済みでした（追加0件）`） |
| 無関係なJSONを読む | 拒否。履歴は12件のまま |
| 壊れたJSONを読む | 拒否。履歴は12件のまま |
| JS例外 | なし |

**未検証**

- **実機（iPhone）では未確認。** iOS Safari の `<a download>` はファイルアプリへの保存になるが、
  この環境から実機を触れないため未検証。**iPhoneで一度、保存と復元を試すこと**
- CDN（Tailwind等）はこの環境から到達できず、ローカル生成のCSSで描画して確認した

### 巻き戻し方

```bash
git checkout <このコミットの1つ前> -- index.html
```

### 反映状況

このコミットのあと `main` にマージし、Vercelの本番へ反映した。

## 2026-08-31 — 名前欄と運勢履歴（端末内保存）を追加

**作業環境**: Claude Code on the web（クラウドセッション）。ブランチ `claude/expand-claude-code-usage-p1v3kl`。

**目的**

「Vercel のおみくじに名前欄を付けて記録を残せるか」を確かめるための第一段階。
サーバーもDBも増やさず、ブラウザの `localStorage` だけで「名前を入れて引く → 履歴が残る」
を成立させた。**サーバーDB（Supabase等）へ移す場合の足場を兼ねている**（下記の設計メモ参照）。

### 変更点

変更したのは **`index.html` のみ**。新規に `CHANGELOG.md` を追加した。

**① 画面 — 名前欄を追加**

- 「確率モード選択」の**上**に `#nameInput`（テキスト入力、最大20文字、任意入力）を置いた
- 空のままでも引ける。空の場合、履歴上の名前は `名無しさん` になる
- 入力するたびに `localStorage` に保存し、次に開いたときは入った状態で始まる
- おみくじを引いている2秒間は、モード選択と同様に入力欄も `disabled` にする

**② 画面 — 履歴ボタンと履歴モーダルを追加**

- 「おみくじを引く」の下に `#historyBtn`（`📜 これまでの運勢を見る（N回）`）を追加。
  **履歴0件のときは `hidden` で隠す**ので、初めて開いた人の画面は今までと変わらない
- `#historyOverlay` を新設。中身は3つ
  - 見出し `<名前>の運勢履歴` と `全N回（この端末に保存されています）`
  - 運勢ごとの回数・割合の横棒グラフ（大吉→中吉→小吉→吉→末吉→凶→大凶の順）
  - 直近20件の一覧（運勢／名前／モード／日時）
- 「履歴を消す」は `confirm()` で1回確認してから消す
- **`z-40` に置いた。** 結果カードの `#resultOverlay` が `z-50` なので、結果表示が常に手前に出る

**③ スクリプト — `historyStore` を追加**

保存に関する処理を `historyStore` という1つのオブジェクトに閉じ込めた。
`load` / `add` / `clear` / `getName` / `setName` の5つだけを外に見せている。

**あえて `async` にしてある。** いまは `localStorage` なので待つ必要はないが、
サーバーのDBに変えると必ず通信待ちが発生する。最初から待つ形にしておけば、
差し替えのとき呼び出し側を書き直さなくて済む。

**④ 安全側の作り**

- `localStorage` はプライベートブラウズや設定次第で例外を投げるため、
  読み書きを `try/catch` で囲み、失敗しても既定値を返す。**おみくじ本体は止まらない**
- 履歴は **200件を上限**とし、古いものから捨てる（`localStorage` の容量上限対策）
- 名前は利用者が自由に入力できるので、一覧に出す前に `escapeHtml()` でエスケープする

### 設計メモ：サーバーDBに移すときに触る場所

`historyStore` の5つの関数の中身だけ。呼び出し側（イベント処理・描画）は変更不要。
ただし移した瞬間に、静的HTMLだけでは済まなくなる点に注意。

- 保存先が端末の外に出るため、**誰でも他人の履歴を読めない仕組み**が別途必要になる
- 名前は自己申告なので、これだけでは「本人が引いた」証明にはならない
- 日時も端末の時計に依存する。記録として意味を持たせるならサーバー側で時刻を打つ必要がある

### 検証

Chromium（Playwright、390×844のスマホ相当画面）で確認した。

| 確認したこと | 結果 |
|---|---|
| 名前を入れて3回引く → 履歴3件が `localStorage` に入る | OK |
| 履歴ボタンの件数表示 | `📜 これまでの運勢を見る（3回）` |
| 履歴モーダルの見出し | `とうだの運勢履歴` ／ `全3回（この端末に保存されています）` |
| 名前を空にしたときの見出し | `名無しさんの運勢履歴` |
| リロード後に名前欄と履歴が残る | OK |
| 「履歴を消す」→ 0件・`履歴がありません` 表示 | OK |
| 名前に `<img src=x onerror=alert(1)>` を入れる | 一覧に `img` 要素が生成されない（エスケープ成功） |
| JS例外 | なし |
| 「おみくじを引く」ボタンの下端 | 469px（画面高844pxに収まる。縦スクロールなし） |

**未検証**

- **実機（iPhone）では未確認。** クラウドの実行環境から実機を触れないため
- **Tailwind CSS / canvas-confetti / Google Fonts のCDNは、この環境からは到達できなかった**
  （組織のプロキシで遮断）。検証時はローカルで生成したTailwind CSSに差し替えて描画した。
  クラス名は既存箇所と同じ書き方に揃えてあるが、**本番の見た目はVercelのプレビューで確認すること**
- 音声まわりには触れていない

### 巻き戻し方

`index.html` の変更を戻すだけでよい（他のファイルには影響しない）。

```bash
git checkout <このコミットの1つ前> -- index.html
```

保存済みの履歴を消したい場合は、ブラウザの開発者ツールで
`localStorage.removeItem('omikuji.history.v1')` と `localStorage.removeItem('omikuji.name.v1')`。

### 反映状況

- **`main` には入れていない。** ブランチ `claude/expand-claude-code-usage-p1v3kl` に push しただけ
- したがって**本番URLの見た目は変わっていない**。確認は Vercel のプレビューURLで行う
