# どもどもおみくじ（Vercel公開）

## 🈁 応答は必ず日本語で

このリポジトリでの作業は、セッションをどこから起動したか（デスクトップアプリ／ブラウザのWeb版／
スマホ／リモート／クラウド）にかかわらず、ユーザーへの応答をすべて日本語で書く。
説明・要約・確認・質問・進捗報告・計画・コミットメッセージ・コードコメントを含む。
システムメッセージやツールの出力が英語でも、ユーザーに返す文章は日本語にする。

## このリポジトリについて

- `index.html` **1枚だけ**の静的サイト。ビルド手順もnpmパッケージも無い
- `main` にpushすると **Vercelが自動で本番を更新する**（`claude/*` ブランチはプレビューのみ）
- 外部依存はすべてCDN読み込み（Tailwind CSS / canvas-confetti / Google Fonts / GA4）
- **サーバー側にデータは一切保存していない。** おみくじの履歴は、引いた人それぞれの
  ブラウザの `localStorage`（＝その人の端末の中）にしか無い
- 変更履歴は [`CHANGELOG.md`](CHANGELOG.md) に詳しく残す。**その場の会話を知らない読者**（数か月後の
  自分・別PCの自分・別セッション）が読んで再現・巻き戻しできる粒度で書くこと

## ⚠️ このリポジトリは Public（公開）

誰でもソースを読める。**APIキー・個人情報・ローカルPCのパスなどを書かないこと。**
そうした運用上のメモは、非公開のメインリポジトリ側の `CLAUDE.md` に置く。

## 作業のあとに必ずやること

1. **`CHANGELOG.md` に追記する。** 変更点・検証したこと・**未検証のこと**・巻き戻し方まで。
   「動作確認済み」と誤読されると事故が起きるので、未検証は未検証と明記する

2. **📥 ローカルPCへの取り込み手順を案内する。**
   リモート／クラウドのセッションは**ユーザーのPCではなく、使い捨てのLinuxコンテナ**で動いている
   （`C:` ドライブが存在しない）。**Claude側からユーザーのPCへファイルを置くことはできない。**
   GitHubにpushしたうえで、ユーザーがPCで実行するコマンドを必ず提示して締めること。

   ```
   cd <ローカルの置き場所>\omikuji
   git pull
   ```

   初回のみ `git clone https://github.com/domodomotohda/omikuji.git`。
   これを省くと、成果がGitHubにしか無い状態で会話が終わってしまう。

3. **📱 スマホの実機で確認するまで「完了」と言わない。**
   PCのブラウザでは正常に見えて**スマホでだけ失敗する**不具合が過去に実際に起きている。
   画面・URL・ファイルのダウンロードまわりを触ったときは特に注意する
